diff --git a/dist/lib/generators/extensions/agent-files.js b/dist/lib/generators/extensions/agent-files.js
index 226d8ed11b46eb4d5efb1f917748dff84d215ba2..0ac332c63f37bb3008c127fdb2287b723384ac72 100644
--- a/dist/lib/generators/extensions/agent-files.js
+++ b/dist/lib/generators/extensions/agent-files.js
@@ -670,7 +670,19 @@ ${indent}  ],`);
     `${indent}},`
   ].join("\n");
 }
-function renderManifest(agents) {
+/**
+ * Where the manifest's two type imports point. They must resolve FROM the
+ * directory the manifest is written to, and those directories differ:
+ * in the monorepo it sits next to the module (`agent_orchestrator/generated/`),
+ * in a standalone app it sits in `<app>/.mercato/generated/`, from which
+ * `../lib/*` is not the module at all. Standalone therefore addresses the
+ * enterprise package by its published subpath.
+ */
+function manifestTypeImportBase(standalone) {
+  return standalone ? "@open-mercato/enterprise/modules/agent_orchestrator/lib" : "../lib";
+}
+function renderManifest(agents, standalone) {
+  const typeImportBase = manifestTypeImportBase(standalone);
   const descriptors = agents.map((agent) => renderDescriptor(agent, "  ")).join("\n");
   return `// AUTO-GENERATED by mercato generate registry \u2014 DO NOT EDIT BY HAND.
 //
@@ -682,8 +694,8 @@ function renderManifest(agents) {
 // \`compileOutcome\` at load time and registers it with \`runtime:'opencode'\`.
 //
 // Regenerate with \`yarn generate\`.
-import type { JsonSchemaNode, OutcomeKind } from '../lib/sdk/outcomeSchema'
-import type { AgentTokenUsage, FileAgentFile } from '../lib/tokens/types'
+import type { JsonSchemaNode, OutcomeKind } from '${typeImportBase}/sdk/outcomeSchema'
+import type { AgentTokenUsage, FileAgentFile } from '${typeImportBase}/tokens/types'
 
 export type FileAgentScript = {
   name: string
@@ -880,7 +892,7 @@ function createAgentFilesExtension(resolver) {
       }
       const { manifestPath, dockerAgentsDir, dockerSkillsDir } = targets;
       fs.mkdirSync(path.dirname(manifestPath), { recursive: true });
-      fs.writeFileSync(manifestPath, renderManifest(sorted), "utf8");
+      fs.writeFileSync(manifestPath, renderManifest(sorted, Boolean(resolver && !resolver.isMonorepo())), "utf8");
       fs.mkdirSync(dockerAgentsDir, { recursive: true });
       const desiredFiles = /* @__PURE__ */ new Map();
       for (const agent of sorted) {
diff --git a/src/lib/generators/extensions/agent-files.ts b/src/lib/generators/extensions/agent-files.ts
index 65ce4381112598e17c1de02522d3cd91795f6c41..77451caa3a87ddd9ac09c9934f115b9f58dc7de1 100644
--- a/src/lib/generators/extensions/agent-files.ts
+++ b/src/lib/generators/extensions/agent-files.ts
@@ -1091,7 +1091,22 @@ function renderDescriptor(agent: DiscoveredAgent, indent: string): string {
   ].join('\n')
 }
 
-function renderManifest(agents: DiscoveredAgent[]): string {
+/**
+ * Where the manifest's two type imports point. They must resolve FROM the
+ * directory the manifest is written to, and those directories differ:
+ * in the monorepo it sits next to the module (`agent_orchestrator/generated/`),
+ * in a standalone app it sits in `<app>/.mercato/generated/`, from which
+ * `../lib/*` is not the module at all. Standalone therefore addresses the
+ * enterprise package by its published subpath.
+ */
+function manifestTypeImportBase(standalone: boolean): string {
+  return standalone
+    ? '@open-mercato/enterprise/modules/agent_orchestrator/lib'
+    : '../lib'
+}
+
+function renderManifest(agents: DiscoveredAgent[], standalone: boolean): string {
+  const typeImportBase = manifestTypeImportBase(standalone)
   const descriptors = agents.map((agent) => renderDescriptor(agent, '  ')).join('\n')
 
   return `// AUTO-GENERATED by mercato generate registry — DO NOT EDIT BY HAND.
@@ -1104,8 +1119,8 @@ function renderManifest(agents: DiscoveredAgent[]): string {
 // \`compileOutcome\` at load time and registers it with \`runtime:'opencode'\`.
 //
 // Regenerate with \`yarn generate\`.
-import type { JsonSchemaNode, OutcomeKind } from '../lib/sdk/outcomeSchema'
-import type { AgentTokenUsage, FileAgentFile } from '../lib/tokens/types'
+import type { JsonSchemaNode, OutcomeKind } from '${typeImportBase}/sdk/outcomeSchema'
+import type { AgentTokenUsage, FileAgentFile } from '${typeImportBase}/tokens/types'
 
 export type FileAgentScript = {
   name: string
@@ -1336,7 +1351,7 @@ export function createAgentFilesExtension(resolver?: AgentFilesResolver): Genera
 
       const { manifestPath, dockerAgentsDir, dockerSkillsDir } = targets
       fs.mkdirSync(path.dirname(manifestPath), { recursive: true })
-      fs.writeFileSync(manifestPath, renderManifest(sorted), 'utf8')
+      fs.writeFileSync(manifestPath, renderManifest(sorted, Boolean(resolver && !resolver.isMonorepo())), 'utf8')
 
       fs.mkdirSync(dockerAgentsDir, { recursive: true })
       const desiredFiles = new Map<string, string>()
